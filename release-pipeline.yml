trigger:
- none

resources:
  pipelines:
  - pipeline: Counters
    source: Build-Pipeline
    trigger: 
      branches:
      - main

jobs:
- deployment: Deploy
  displayName: Deploy
  environment: Counters Environment
  pool: 
    name: Oracle VM
  strategy:
    runOnce:
      deploy:
        steps:          
        - download: Build-Pipeline
          artifact: drop
          displayName: "download artifact"
        - task: ExtractFiles@1
          inputs:
            archiveFilePatterns: "$(PIPELINE.WORKSPACE)/Build-Pipeline/drop/$(resources.pipeline.Counters.runID).zip"
            destinationFolder: "$(agent.builddirectory)"
            cleanDestinationFolder: false
          displayName: "extract files"
        - script: |  
            echo "Starting App in prod mode on port 5000"
            $(PORT_PROD) $(URL_PROD) npm run prod
          displayName: "npm run prod"